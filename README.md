# менеджер заявок

## запуск

```commandline
cp .env.example .env
cp ./backend/.env.example ./backend/.env
cp ./frontend/.env.example ./frontend/.env
docker compose up -d
```

## backend
[документация Swagger](http://localhost:8000/docs)

в качестве авторизации используется HTTP Basic Auth
